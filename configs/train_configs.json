{
    "dataset_root_dir": "datasets",
    "pretrained_model_dir": "pretrained_models",
    "pretrained_sp_model_dir": "pretrained/superpoint_v1.pth",
    "output_dir": "outputs",

    "device": "cuda",
    "num_workers": 4,
    "batch_size": 32,
    "batch_size_test": 32,
    "start_epoch": 0,
    "num_epochs": 100,
    "lr": 0.001,

    "save_epoch": 10,
    "save_optimizer": false
}